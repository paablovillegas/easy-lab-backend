const { Router } = require("express");

const router = Router();

router.get('/', );
router.get('/:uid', );
router.post('/', );
router.put('/:uid', );
router.delete('/:uid', );

module.exports = router;